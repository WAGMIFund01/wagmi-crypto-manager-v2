import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint is now configured to use warnings instead of errors
  // This allows builds to succeed while still providing code quality feedback
};

export default nextConfig;
